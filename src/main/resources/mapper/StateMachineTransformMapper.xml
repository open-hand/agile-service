<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.choerodon.agile.infra.mapper.StateMachineTransformMapper">

    <select id="queryById" resultType="io.choerodon.agile.infra.dto.StateMachineTransformDTO">
        SELECT
        smt.*,
        smn.status_id as endStatusId
        FROM fd_state_machine_transform smt
        LEFT JOIN fd_state_machine_node smn ON smn.id = smt.end_node_id
        where smt.organization_id = #{organizationId}
        and smt.id = #{id}
    </select>

    <update id="deleteByNodeId">
        DELETE FROM fd_state_machine_transform
        WHERE start_node_id = #{nodeId} OR end_node_id = #{nodeId}
    </update>

    <delete id="deleteByStateMachineIdAndNodeId">
        DELETE FROM fd_state_machine_transform
        WHERE
        organization_id = #{organizationId}
        AND state_machine_id = #{stateMachineId}
        AND (
           start_node_id = #{nodeId} OR end_node_id = #{nodeId}
        )
    </delete>

    <select id="queryByStartNodeIdOrType" resultType="io.choerodon.agile.infra.dto.StateMachineTransformDTO">
        SELECT *
        FROM fd_state_machine_transform
        WHERE organization_id = #{organizationId}
        and state_machine_id = #{stateMachineId}
        and (
          start_node_id = #{startNodeId}
          OR
          type = #{transformType}
        )
    </select>

    <select id="queryByStateMachineIds" resultType="io.choerodon.agile.infra.dto.StateMachineTransformDTO">
        SELECT
        smt.*,
        smn.status_id as endStatusId
        FROM fd_state_machine_transform smt
        LEFT JOIN fd_state_machine_node smn ON smn.id = smt.end_node_id
        where smt.organization_id = #{organizationId}
        and smt.state_machine_id in
        <foreach collection="stateMachineIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        insert into fd_state_machine_transform(name,description,state_machine_id
        ,start_node_id,end_node_id,type,condition_strategy,organization_id,created_by,last_updated_by)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.name},#{item.description},#{item.stateMachineId}
            ,#{item.startNodeId},#{item.endNodeId}
            ,#{item.type},#{item.conditionStrategy}
            ,#{item.organizationId},#{item.createdBy},#{item.lastUpdatedBy})
        </foreach>
    </insert>
</mapper>
