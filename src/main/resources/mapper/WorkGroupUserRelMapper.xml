<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.choerodon.agile.infra.mapper.WorkGroupUserRelMapper">

  <delete id="deleteByWorkGroupIds">
      DELETE FROM fd_work_group_user_rel
      WHERE organization_id = #{organizationId}
      AND work_group_id IN
      <foreach collection="workGroupIds" item="workGroupId" open="(" close=")" separator=",">
          #{workGroupId}
      </foreach>
  </delete>

  <delete id="batchDelete">
      DELETE FROM fd_work_group_user_rel
      WHERE organization_id = #{organizationId}
      AND work_group_id in
      <foreach collection="workGroupIds" item="workGroupId" open="(" close=")" separator=",">
          #{workGroupId}
      </foreach>
      AND user_id in
      <foreach collection="userIds" item="userId" open="(" close=")" separator=",">
          #{userId}
      </foreach>
  </delete>

   <select id="queryByWorkGroupId" resultType="java.lang.Long">
       select user_id
       from fd_work_group_user_rel
       where organization_id = #{organizationId}
       <if test="workGroupId != null">
           and work_group_id = #{workGroupId}
       </if>
   </select>

    <select id="listUserIdsByWorkGroupIds" resultType="java.lang.Long">
        select user_id
        from fd_work_group_user_rel
        where organization_id = #{organizationId}
        <if test="workGroupIds != null and workGroupIds.size > 0">
            and work_group_id in
            <foreach collection="workGroupIds" item="workGroupId" open="(" close=")" separator=",">
                #{workGroupId}
            </foreach>
        </if>
    </select>
</mapper>